services:
  meld-control:
    build: .
    container_name: meld-control
    ports:
      - "3000:3000"
    environment:
      - MELD_HOST=host.docker.internal
      - MELD_PORT=13376
      - HTTP_PORT=3000
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
